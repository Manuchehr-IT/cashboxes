FROM node:20-slim

WORKDIR /app

COPY package.json package-lock.json ./

RUN npm ci

# ARG/ENV идут после npm ci намеренно — см. Dockerfile.production.
ARG VITE_API_URL
ENV VITE_API_URL=$VITE_API_URL

COPY . .

CMD ["npm", "run", "dev"]
